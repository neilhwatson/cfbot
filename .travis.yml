language: perl
perl:
   - '5.16'
   - '5.18'
   - '5.20'
   - '5.22'

install:
   - cpanm -n Test::More Bot::BasicBot Perl6::Slurp AnyEvent::Util HTTP::Tiny
   - cpanm -n Cache::FastMmap Web::Query XML::Feed Config::YAML JSON 
   - cpanm -n Mojolicious Git::Repository
   - sudo apt-get install ngircd 
   - chmod -R g-w .

script:
   - prove

sudo: required

notifications:
   - email: false
